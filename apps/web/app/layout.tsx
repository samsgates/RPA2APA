import './styles.css';
export const metadata = { title: 'RPA2APA Review Studio', description: 'Governed RPA to APA migration review' };
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
